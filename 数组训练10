#include <stdio.h>

int main() {
    int M, N;
    scanf("%d %d", &M, &N);
    
    int cnt[10] = {0};  // 初始化计数器数组
    
    for (int i = M; i <= N; i++) {
        int num = i;
        // 分解数字的每一位
        while (num > 0) {
            int digit = num % 10;  // 取出最后一位
            cnt[digit]++;          // 对应数字计数加1
            num = num / 10;        // 去掉最后一位
        }
    }
    
    // 输出结果
    for (int i = 0; i < 10; i++) {
        printf("%d", cnt[i]);
        if (i < 9) {
            printf(" ");
        }
    }
    printf("\n");
    
    return 0;
}
